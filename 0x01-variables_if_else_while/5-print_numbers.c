#include <stdio.h>
/* more headers goes there */
/**
 * main - Program entry point
 * Return: 0 on success. Error code otherwise
 */
int main(void)
{
int i;
for (i = 0; i <= 9; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
